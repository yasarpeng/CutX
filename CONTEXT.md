# CutX

跨平台离线文件切割与合并工具。将大文件按指定大小切分为多个分片，传输到离线环境后合并还原。

## Language

**分片 (Chunk)**:
大文件切割后的小文件片段。分片是原始数据的直接字节切分，不压缩、不加密。
_Avoid_: 块(block)、段(segment)、片(slice)

**清单文件 (Manifest)**:
切割时自动生成的 JSON 文件，记录原始文件元数据和所有分片的校验信息。是合并操作的必要输入。
_Avoid_: 索引文件、描述文件、metadata文件

**校验和 (Hash)**:
用 MD5 或 SHA-256 算法计算的文件指纹，用于验证数据完整性。清单中始终包含每个分片的校验和和整体校验和。
_Avoid_: 指纹、摘要、签名

**quick 模式**:
merge 的默认模式。纯 I/O 合并，不计算任何校验和。速度最快。
_Avoid_: 快速模式、普通模式

**verify 模式**:
merge 的可选模式（`--mode verify`）。边写边校验，合并时同时计算校验和并与清单比对。数据完整性保障。
_Avoid_: 安全模式、校验模式

**边写边校验**:
合并时在写入每个分片的同时计算该分片的校验和。校验失败立即停止写入并删除输出文件。verify 模式的核心机制。
_Avoid_: 实时校验、流式校验

**单遍计算**:
split 在同一遍文件读取中同时计算分片校验和和整体校验和。不额外读取数据，hash 计算几乎零成本。
_Avoid_: 单次哈希、一遍式校验

**输出子目录 (Output Subdirectory)**:
split 默认将分片文件和清单文件输出到一个自动创建的子目录中，而非散落在当前目录。子目录名格式为 `cutx-<源文件名去除扩展名>`。分片和清单文件都存放在此子目录内。使用 `-o` 参数可覆盖默认输出路径。
_Avoid_: 输出目录、分片目录

**GUI 包装器 (GUI Wrapper)**:
V2.0 计划中的 Windows 专用图形界面版本（`CutX.GUI.exe`），基于 WinUI 3 + Windows App SDK，采用 Fluent Design 深色主题。GUI 作为 C# shell 调用 cutx.exe CLI 二进制，通过 CutxProcessService 解析 stderr 输出获取进度和日志。核心逻辑保持 Go 实现，GUI 不重复业务逻辑。全功能：split/merge/verify 三大操作及主要参数均有图形界面入口。子目录行为与 CLI 一致。清单格式与 CLI 版本完全兼容。
_Avoid_: 图形版本、桌面版
